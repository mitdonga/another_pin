<div class="product">
	<div>
		<%= link_to product_path(product) do %>
			<%= image_tag product.image_1.medium.url %>
		<% end %>
		<h3><%= link_to product.title, product_path(product) %> </h3>
		<p> <%= number_to_currency product.number_to_usd %> </p>
	</div>
</div>